module.exports = class Cart {
  static addProduct(id) {
    // Fetch(đọc dữ liệu thông qua Link) giỏ hàng hiện có
    // Xác nhận trong giỏ hàng => Tìm sản phẩm đã tồn tại
    // Thêm sản phẩm mới/ tăng số lượng
  }
};
